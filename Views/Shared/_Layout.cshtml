@{
    Layout = null;
}
@inject Microsoft.Extensions.Configuration.IConfiguration Configuration
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>BCMTrac Authentication</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css" />
    @RenderSection("HeadStyles", required: false)
</head>
<body class="bcm-bg">
    <div class="bg-overlay"></div>
    <div class="container-fluid vh-100 d-flex justify-content-center align-items-center">
        <div class="auth-container shadow-glass animate-pop">
            <div class="text-center mb-4">
                <div class="brand-lockup">
                    <img class="brand-logo-img" src="/assets/logo2.png" alt="BCMTrac" onerror="this.style.display='none'">
                    <span class="brand-name">BCMTrac</span>
                </div>
            </div>
            <div id="alert-placeholder"></div>
            @RenderBody()
            <div class="mt-4 text-center security-note">
                <i class="fa-solid fa-lock"></i> Bank-grade encryption • POPIA compliant
            </div>
        </div>
    </div>
    <script>
        window.GOOGLE_CLIENT_ID = '@(Configuration["Google:ClientId"] ?? "")';
    </script>
    <script src="https://accounts.google.com/gsi/client" async defer></script>
    <script src="/js/bg.js"></script>
</body>
</html>
